# 参与贡献

感谢您帮助改进 Agent Protocols。本仓库包含协议规范，因此所有变更都应在互操作性、安全性和长期兼容性方面进行评审。

## 贡献类型

有价值的贡献包括：

- 澄清模糊的规范性语言。
- 提出协议变更建议（附带兼容性说明）。
- 增加示例、图表、测试向量、JSON Schema 或一致性测试。
- 报告独立客户端、Host 或 SDK 的实现经验。
- 保持英文和简体中文文档的对齐。

## 规范用语

`MUST`、`MUST NOT`、`SHOULD`、`SHOULD NOT` 和 `MAY` 是规范性用语。修改这些词汇的 Pull Request 属于行为变更，应解释变更的原因。

## Pull Request 自检清单

在提交 Pull Request 之前，请检查：

- 变更范围仅限于一个协议或一个明确的跨协议关注点。
- 当规范行为发生变更时，英文和简体中文版本都已更新。
- 已描述向后兼容性。
- 已描述安全性和滥用影响。
- 新的标识符、字段和事件类型已稳定且命名一致。
- 示例在相关时使用了 `did:agent:` Agent ID 和签名事件信封。

## 兼容性预期

草案规范可能会发生变化，但应避免不必要的频繁变动。尽可能优先选择增量式变更。破坏性变更应指明迁移影响以及是否需要新的协议主版本。

## Issue 讨论

在提出协议变更建议时，请包含：

1. 问题陈述。
2. 受影响的协议文档。
3. 建议的行为。
4. 考虑过的替代方案。
5. 互操作性影响。
6. 安全和隐私影响。

## 语言对齐

英文文档是跨实现评审的默认文本。简体中文文档应保持相同的规范要求。如果您只能更新其中一种语言，请在 Pull Request 中说明，以便维护者协助对齐另一个版本。
